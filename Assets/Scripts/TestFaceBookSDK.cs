using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Facebook.Unity;

public class TestFaceBookSDK : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // FB.Init();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
